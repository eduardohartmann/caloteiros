export default function Brand() {
  return (
    <a className="brand" href="#" aria-label="Caloteiros" onClick={(event) => event.preventDefault()}>
      <span className="brand-mark">C</span>
      <span>Caloteiros</span>
    </a>
  );
}
